[project]
name = "openclaw-social-mcp"
version = "0.1.0"
description = "MCP server exposing social-drafting tools to an OpenClaw agent."
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=2.0",
    "httpx>=0.27",
    "openai>=1.50",
    "pydantic>=2.8",
]

[tool.uv]
package = false
